#include<stdio.h>
void main(){
	//Write a C program to input the length and width of a rectangle and find its perimeter.
	int len = 3, wid = 5, p;
	p = 2*(len+wid);
	printf("Perimeter of rectangle of length %d and width %d = %d",len,wid,p);
	
}