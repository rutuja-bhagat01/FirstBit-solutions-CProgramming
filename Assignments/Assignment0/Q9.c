#include<stdio.h>
void main(){
	//Write a C program to input the base and height of a triangle and calculate its area.
	int base = 4, height = 7;
	float area = (1/2.0f)*(base*height);
	 
	printf("Area of triangle = %0.2f",area);
}